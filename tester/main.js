import { run } from './index.js';

//const concurrent = true;
const concurrent = false;
run('http://localhost:3000', concurrent);
//run('http://waygate.io:3000', concurrent);
//run('https://os.anderspitman.com', concurrent);
